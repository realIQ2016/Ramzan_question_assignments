// Write a program to determine the greater of two numbers.
var determine_num1 = 30; //canditoin 1 
var determine_num2 = 50; //canditoin 2
if (determine_num1 > determine_num2) {
    console.log("".concat(determine_num1, ":number is greater than number ").concat(determine_num2));
}
else
    console.log("".concat(determine_num1, ":number is greater than number:").concat(determine_num2));
