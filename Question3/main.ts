// Write a program to determine the greater of two numbers.
const determine_num1:number=30;//canditoin 1 

const determine_num2:number=50;//canditoin 2

if(determine_num1 > determine_num2){

    console.log(`${determine_num1}:number is greater than number ${determine_num2}`);
}
else
console.log(`${determine_num1}:number is greater than number:${determine_num2}`);
