// Write a simple JavaScript program that checks if a person is eligible for a 
//CNIC (show a message with the person's name).
var username = "Qamar Ali Shah";
var userAge = 16;
if (userAge >= 18) {
    console.log("".concat(username, " is eligible for NIC."));
}
else {
    console.log("".concat(username, " is not elighble for NIC."));
}
