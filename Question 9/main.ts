// Write a simple JavaScript program that checks if a person is eligible for a 
//CNIC (show a message with the person's name).
const username = "Qamar Ali Shah";

const userAge = 16;

if (userAge>= 18){
    console.log(`${username} is eligible for NIC.`);
} 
else{console.log(`${username} is not elighble for NIC.`);}